---
created: 2026-08-12
tags: [meta, registro, sesion]
---

# Registro — 2026-08-12

> Delta desde 2026-08-10 (último registro): sesiones S2–S5 del repo Suplehuges (ver [[10 - Proyectos/Suplehuges/Suplehuges]]).

## Qué se hizo

- **S2 — Prototipo 1** (commit `01dd061`, 09/ago): tienda con chips filtro `?cat=`, favoritos (`sh_favs`), vista producto `?id=` con thumbs, 27 corazones accesibles, auditoría 0 issues.
- **S3 — Prototipo 1.1** (`9639da9`, 09/ago): 5 correcciones del dueño + barrido de bugs; hero sin estadísticas ("ROPA DE ENTRENAMIENTO DE ÉLITE"); sección POR QUÉ SUPLEHUGES; efecto NEON v2; footer con marcas.
- **S4** (`e9c3ff4`, 12/ago): pulido pedido/drops/producto (timeline fijo, self-link carrito arreglado, video hero pausado en vistas ocultas); marquee de anuncio (PRUEBA) en 5 vistas; audit 0 issues.
- **S5 visual** (12/ago): botones 3D hard-shadow brutalistas, footer/bottom-nav/FAB WhatsApp a nivel global; **marquee APROBADO por el dueño** (ya no es PRUEBA; home sin marquee). Audit 0 issues, func OK, shot_check 0 errores, 4 revisiones MiMo TODO OK.
- **S5 — F0 migración fuera de Shopify** (commit `2641d32`, 12/ago): repo sin dominios reales — config-driven (`js/config.js` + `config.local.js` gitignored), 44 URLs → centinelas `data-src="assets/img/"`, catálogo embebido como fallback (`FALLBACK_PRODUCTS`), `supabase/schema.sql` (7 tablas + RLS), `scripts/seed_catalog.cjs` + `scripts/fetch_images.cjs` (fuente en `.env` gitignored), stubs `demo-v2/api/mp-webhook.cjs` + `paypal-webhook.cjs`, runbook `.qa/migracion.md`, AGENTS.md del repo actualizado. Verificado: func_check OK; audit en estado intermedio (imágenes esperan credenciales).
- **Vault**: documentación del proyecto Suplehuges actualizada al día (este registro + STATUS + MOC + 4 notas técnicas: Arquitectura, Decisiones Técnicas, Deployment, Seguridad-Auditoría).
- **S5 visual — verificación final y commit** (commit `a25fbdd` + chore `cf830b9`): revisión de la vault detectó el delta pendiente; AGENTS.md del repo corregido (F0 ya commiteado en `2641d32`) y commiteada la verificación S5 visual (AGENTS.md + 16 shots `shots/redesign/` + 14 shots `s5_*` nuevos en `.qa/images/`). Detalle de la sesión:
  - Fix a `audit.cjs`: check de truncado ahora por `document.createRange()` (ignora pseudo-elementos `::before` del hard-shadow); imágenes con `src=""` (sentinel sin config real) ya no cuentan como rotas. Audit final: **0 issues**, contrastes OK (white_on_red 4.75, white_on_green 1.91, black_on_amber 8.95).
  - Fix a `func_extra.cjs`: marquee verificado por `.mq-row` dentro de `.view:not([hidden])` (el atributo `data-prueba` ya no existe); corregido error de serialización en `page.evaluate` y corrupción de acentos al pasar código por pipe de PowerShell.
  - Verificación completa: func_check OK (tienda 23 cards, `?cat=devant` 7, favoritos, producto), shot_check 0 errores, footer/bottom-nav/FAB visibles en las 6 vistas @390px.
  - 4 revisiones MiMo TODO OK (hard-shadow, hover con presión, footer desktop/mobile, marquee drops). 1 falso positivo resuelto: shot de footer mobile parecía tapar el copyright, geometría real 60px libres.
  - Herramienta: MiMo se cuelga con 2-3 imágenes en un solo `opencode run` (timeout 420s); con 1 imagen funciona. Bug Bun `panic: Illegal instruction` (cliente).

## Qué falta

- [ ] Proyecto Supabase del cliente (bloquea imágenes y shots visuales) → ejecutar runbook F0 (`.qa/migracion.md`).
- [ ] F1: panel admin noir (`demo-v2/admin/`).
- [ ] Deploy demo-v2 a Vercel (Root Directory `demo-v2`).
- [ ] Mostrar demo a Santiago + entregar propuesta/guion y agendar llamada.
- [ ] Confirmar handle real de Instagram; línea base de métricas.
- [ ] Respaldo de la vault a GitHub (pendiente global).
- [x] S5 visual verificado y commiteado (`a25fbdd`) — working tree del repo limpio.

## Decisiones

- **Migración TOTAL fuera de Shopify** (no complemento): Supabase + Vercel, infra $0/mes, pagos múltiples (Mercado Pago, PayPal, Apple/Google Pay, tarjeta, SPEI, OXXO).
- Regla dura: **nunca commitear dominios de producción** (valores reales solo en archivos gitignored).
- Panel admin noir multiusuario (Santiago decide accesos).
- Fase del proyecto en la vault: se mantiene `propuesta-comercial` (la venta a Santiago sigue siendo el objetivo; F0 avanza como preparación técnica).
