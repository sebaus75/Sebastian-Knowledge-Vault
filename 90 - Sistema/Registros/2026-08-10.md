---
created: 2026-08-10
tags: [meta, registro, sesion]
---

# Registro — 2026-08-10

## Qué se hizo

- **MCP conectado**: plugin Obsidian Local REST API v5.0.2 en `127.0.0.1:27124`. Certificado TLS autofirmado importado + `NODE_EXTRA_CA_CERTS` a nivel usuario (`C:\Users\sebas\.config\opencode\certs\obsidian-local-rest-api.crt`). Pendiente: reiniciar OpenCode Desktop para activar las 16 herramientas MCP.
- **Auditoría de enlaces**: script `link_audit.mjs` → 80 wikilinks ambiguos/rotos corregidos a rutas completas desde raíz (`[[10 - Proyectos/...]]`). Resultado final: 0 problemas.
- **Frontmatter normalizado**: `created` agregado a 21 notas (Wolf Barber 2026-07-24, TemplateTest 2026-07-25, Home/VAULT/Desarrollo Web 2026-07-25); tags en Home, VAULT, Desarrollo Web - Index. Corregido typo `2025-07-24` → `2026-07-24` en MOC de Wolf Barber.
- **Grafo visual**: snippet `.obsidian/snippets/graph.css` (tema oscuro/claro, nodos por tag, glow) activado en `appearance.json`; 8 color groups en `graph.json` (proyecto/activo, suplehuges, barberia, template, recurso/ia, recurso/obsidian, concepto, meta). Nota: reiniciar Obsidian para recargar (pestaña abierta sobrescribiría).
- **Sistema cerebro instalado**: `AGENTS.md` (contrato), `CONTEXT.md` (estado global), `STATUS.md` en los 3 proyectos, `90 - Sistema/` (este registro + panel + plantilla), skill global `vault-brain` en `~/.config/opencode/skills/`, MCP obsidian agregado a la config global de opencode.

## Qué falta

- [ ] Reiniciar OpenCode Desktop (activar MCP) y probar `vault_list` end-to-end.
- [ ] Reiniciar Obsidian (recargar graph.json con color groups).
- [ ] Verificar tags en `30 - Recursos/` para el grafo.
- [ ] Revisar `00 - Inbox/`.
- [ ] Respaldo semanal a GitHub.

## Decisiones

- TemplateTest y Wolf Barber comparten 8 basenames → **no fusionar** (proyectos legítimos distintos); enlaces siempre a ruta completa.
- Fases por proyecto (no enum global): Wolf Barber `produccion`, Suplehuges `propuesta-comercial`, TemplateTest `listo-empaquetar`.
- Root del vault: solo `Home.md`, `CONTEXT.md`, `VAULT.md`, `AGENTS.md`, `.gitignore`.
